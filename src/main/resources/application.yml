server:
  port: 8080

spring:
  config:
    import: optional:classpath:application-local.yml

# Do not put real credentials here. Use application-local.yml or environment variables.
app:
  dashscope:
    api-key: ${DASHSCOPE_API_KEY:}
  oss:
    endpoint: ${ALIYUN_OSS_ENDPOINT:https://oss-cn-beijing.aliyuncs.com}
    region: ${ALIYUN_OSS_REGION:cn-beijing}
    bucket: ${ALIYUN_OSS_BUCKET:}
    access-key-id: ${ALIYUN_OSS_ACCESS_KEY_ID:}
    access-key-secret: ALIYUN_OSS_ACCESS_KEY_SECRET
  imm:
    endpoint: ${ALIYUN_IMM_ENDPOINT:imm.cn-beijing.aliyuncs.com}
    project-name: ${ALIYUN_IMM_PROJECT:pdfReview}
  documents:
    rule-file-url: ${RULE_FILE_URL:}
    requirement-file-url: ${REQUIREMENT_FILE_URL:}
